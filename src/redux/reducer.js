

const intialState = {
    vinos: []
}

const reducer = (state = intialState, { type, payload }) => {
    switch (type) {
        default:
            return {
                ...state
            }
    }
}